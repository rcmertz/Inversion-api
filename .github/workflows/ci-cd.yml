name: Deploy AWS Inversion

on:
    push:
        branches: [docker]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v3